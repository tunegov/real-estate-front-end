import { lighten, darken } from 'polished';

const primaryColor = '#26544B';
const secondaryColor = '#000102';
const tertiaryColor = darken(0.1, '#fff');

const theme = {
  primaryColor,
  secondaryColor,
  tertiaryColor,
};

export default theme;
